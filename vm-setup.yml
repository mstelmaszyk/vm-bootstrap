- name: Configure VM
  hosts: localhost
  connection: local
  become: yes
  become_method: sudo
  become_user: root

  vars:
    ansible_user: "{{ lookup('env', 'USER') }}"
    home_dir: "{{ lookup('env', 'HOME') }}"

  roles:
    - setup
